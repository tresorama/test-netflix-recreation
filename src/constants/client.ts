// export all `constants/shared`
export * from './shared';

// Debug views must be visible ??
export const IS_DEBUG = false;

// Preserve API request limit while developing
export const AVOID_REFETCHING_FROM_REAL_STORAGE = false;